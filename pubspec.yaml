name: expensetracker
description: A Flutter app to track expenses.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  uuid: ^4.0.0
  provider: ^6.1.1
  charts_flutter: ^0.12.0
  intl: ^0.17.0 # Downgraded for compatibility

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
